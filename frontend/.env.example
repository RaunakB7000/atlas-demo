# Leave both empty to use the Vite dev proxy (npm run dev) or the nginx
# proxy inside Docker. Set them only when the API lives on another origin.
VITE_API_URL=
VITE_WS_URL=

# MapTiler Cloud key. Without it the map falls back to free Carto tiles.
VITE_MAPTILER_KEY=
